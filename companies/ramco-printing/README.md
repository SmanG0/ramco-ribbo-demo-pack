# Ramco Printing Works — package PENDING

**Status:** not yet delivered. Spec exists (`build-pack/03-ramco-printing.md`), package does not.

When ready this folder will mirror `companies/safarilink/`:

```
kb/       knowledge base documents (markdown)
seed/     seed data CSVs
scripts/  generate_seed.py — reproducible generator with assertions
README.md
SPEC-CORRECTIONS.md   (if any facts in the spec are corrected during KB authoring)
```

Agent pattern: **structured capture** (Plexus — Print & Packaging vertical). Do not start
this build until the shared spine passes its acceptance checklist.
